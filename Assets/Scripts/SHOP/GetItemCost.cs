using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class GetItemCost : MonoBehaviour
{
    public Text itemCost;
    private GameObject buyableItem;
    // Start is called before the first frame update
    void Start()
    {
        buyableItem  = buyableItem.GetComponent<>
        FindObjectOfType<buyableItem.name>()
        //itemCost.text = buyableItem.
        Debug.Log("buyable item name = " + buyableItem.name);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
